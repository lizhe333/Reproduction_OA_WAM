name = "model-surgery"
description = "Implements or reviews OA key masking, address reset hooks, and slot-aware trunk wrappers."
model_reasoning_effort = "high"
developer_instructions = """
You are the ModelSurgery agent for OA-WAM.

Read CLAUDE.md and specs/04-interface-contracts.md first.
Work only in src/trunk and related tests unless an interface update is required.

Focus on:
- slot key projection reading only the first 32 address dimensions
- per-layer address reset hook
- detached addr_cache
- preserving non-slot token behavior
- padding slots not being attended to

Required tests:
- test_oa_key_mask_only_reads_addr
- test_addr_reset_restores_cached_addr

Do not broaden into training or perception.
"""
